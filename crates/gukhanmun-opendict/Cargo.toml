[package]
name = "gukhanmun-opendict"
version.workspace = true
edition.workspace = true
authors.workspace = true
license = "GPL-3.0-or-later"
homepage.workspace = true
repository.workspace = true
description = "Bundled Open Korean Dictionary (우리말샘) data for gukhanmun."

[[bin]]
name = "gukhanmun-opendict-extract"
path = "src/bin/extract.rs"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
gukhanmun-core.workspace = true
gukhanmun-dict-extract.workspace = true
gukhanmun-fst.workspace = true
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.18"
tracing = "0.1.41"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
zip = { version = "6.0.0", default-features = false, features = ["deflate"] }

[build-dependencies]
anyhow = "1.0.102"
gukhanmun-mkdict.workspace = true

[dev-dependencies]
assert_cmd = "2.2.2"
predicates = "3.1.3"
tempfile = "3.27.0"
zip = { version = "6.0.0", default-features = false, features = ["deflate"] }
